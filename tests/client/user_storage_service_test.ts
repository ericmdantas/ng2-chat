import {
  it,
  xit,
  expect,
  describe,
  xdescribe
} from 'angular2/testing';

/*import {UserStorageService} from '../../app/user/user_storage_service';

xdescribe('user_storage', () => {
  describe('creation', () => {
    it('should be created correctly', () => {
      expect(new UserStorageService()).toBeDefined();
    });
  });

  describe('isOnline', () => {
    it('should return false', () => {
      let _userStorage = new UserStorageService();

      expect(_userStorage.isOnline()).toBe(false);
    });

    it('should return true', () => {
      let _userStorage = new UserStorageService();

      expect(_userStorage.isOnline()).toBe(true);
    });
  });
});*/
