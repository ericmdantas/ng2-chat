import {
  describe,
  it,
  expect,
  injectAsync,
  TestComponentBuilder
} from 'angular2/testing';

import {PromptCmp} from '../../app/prompt/prompt_cmp';

/*describe('prompt-cmp', () => {
  describe('creation', () => {
    it('should have the right template', injectAsync([TestComponentBuilder], (tcb) => {
      return tcb.createAsync(PromptCmp).then((fixture) => {
        fixture.detectChanges();

        let compiled = fixture.debugElement.nativeElement;

        console.log(compiled);
      });
    }));
  });
});*/
