import {
  describe,
  it,
  expect,
  beforeEachProviders
} from 'angular2/testing';

import {
  provide
} from 'angular2/angular2';

/*import {NotificationNewMessagesService} from '../../app/notifications/notifications_new_messages_service';
import {MessageModel} from '../app/chat/message/message_model';
import {MentionService} from '../app/chat/chat_list/mention_service';

class MockMessageModel extends MessageModel {

}

class MockMentionService extends MentionService {

}

describe('notifications_new_messages_service', () => {
  beforeEachProviders(() => [
    provide(MessageModel, {useClass: MockMessageModel}),
    provide(MentionService, {useClass: MockMentionService})
  ]);

  describe('creation', () => {
    it('should have the right values for the static stuff', () => {
      expect(NotificationNewMessagesService.DEFAULT_TITLE).toBe('_');
      expect(NotificationNewMessagesService.REPEATER).toBe(555);
    });
  });
});*/
